apiVersion: v1
kind: Service
metadata:
  name: {{ include "kloud.fullname" . }}
  labels:
    {{- include "kloud.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kloud.selectorLabels" . | nindent 4 }}

