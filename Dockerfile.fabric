FROM python:3.11-slim

WORKDIR /app

RUN apt-get update && apt-get install -y --no-install-recommends curl && rm -rf /var/lib/apt/lists/*

RUN pip install --no-cache-dir fastapi uvicorn httpx

COPY fabric_core.py .
COPY fabric_api.py .

EXPOSE 7800

CMD ["python", "fabric_api.py"]
